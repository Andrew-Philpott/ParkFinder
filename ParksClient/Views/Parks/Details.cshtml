@{
  Layout = "_Layout";
}

@model ParksClient.Models.Park;


<div class="jumbotron">
    <h2 class="display-4">@($"{Model.Name}")</h2>
</div>

<div class='content'>

</div>

<div>@Html.ActionLink("Edit Park", "Edit", new {id = Model.ParkId})</div>
<div>@Html.ActionLink("Delete Park", "Delete", new {id = Model.ParkId})</div>